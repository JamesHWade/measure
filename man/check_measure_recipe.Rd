% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data-organization.R
\name{check_measure_recipe}
\alias{check_measure_recipe}
\title{Check Measure Recipe Structure}
\usage{
check_measure_recipe(recipe, strict = TRUE)
}
\arguments{
\item{recipe}{A recipe object to validate.}

\item{strict}{Logical. If TRUE (default), returns errors as a tibble.
If FALSE, issues cli warnings and returns the recipe invisibly.}
}
\value{
If \code{strict = TRUE}, returns a tibble with columns:
\describe{
\item{level}{Severity: "error", "warning", or "info"}
\item{check}{Name of the check that triggered the message}
\item{message}{Description of the issue}
}
If \code{strict = FALSE}, returns the recipe invisibly after printing warnings.
}
\description{
Validates that a recipe is properly structured for measure operations.
Checks for common issues like missing input steps, incompatible column
types, and role conflicts.
}
\details{
The following checks are performed:

\strong{Errors} (will cause failures):
\itemize{
\item No input step (\verb{step_measure_input_*})
\item Output step before input step
\item Multiple input steps
}

\strong{Warnings} (may cause issues):
\itemize{
\item No output step (data stays in internal format)
\item Processing steps after output step
\item No predictor columns identified
}

\strong{Info} (suggestions):
\itemize{
\item Large number of measurement columns (consider dimension reduction)
\item No ID column identified
}
}
\examples{
\dontrun{
library(recipes)

# Check a properly structured recipe
rec <- recipe(outcome ~ ., data = meats_long) |>
  update_role(id, new_role = "id") |>
  step_measure_input_long(transmittance, location = vars(channel)) |>
  step_measure_snv() |>
  step_measure_output_wide()

check_measure_recipe(rec)

# Check a recipe with issues
bad_rec <- recipe(outcome ~ ., data = my_data) |>
  step_measure_snv()  # Missing input step!

check_measure_recipe(bad_rec)
}

}
