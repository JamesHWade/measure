% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/precision.R
\name{measure_gage_rr}
\alias{measure_gage_rr}
\title{Gage R&R (Measurement System Analysis)}
\usage{
measure_gage_rr(
  data,
  response_col,
  part_col,
  operator_col,
  tolerance = NULL,
  conf_level = 0.95,
  k = 5.15
)
}
\arguments{
\item{data}{A data frame containing Gage R&R study data.}

\item{response_col}{Name of the column containing the measurements.}

\item{part_col}{Name of the column identifying parts/samples.}

\item{operator_col}{Name of the column identifying operators/analysts.}

\item{tolerance}{Optional specification tolerance for calculating \%Study
variation and \%Tolerance.}

\item{conf_level}{Confidence level. Default is 0.95.}

\item{k}{Multiplier for study variation calculation. Default is 5.15 (99\%).}
}
\value{
A \code{measure_gage_rr} object containing:
\itemize{
\item Variance components (Repeatability, Reproducibility, Part-to-Part)
\item \%Contribution of each component
\item \%Study Variation (using k * sigma)
\item \%Tolerance (if tolerance provided)
\item Number of distinct categories (ndc)
}
}
\description{
Performs a Gage Repeatability and Reproducibility study to assess
measurement system variation.
}
\details{
Gage R&R decomposes total measurement variation into:
\itemize{
\item \strong{Repeatability (EV)}: Equipment variation - variability from repeated
measurements by the same operator on the same part
\item \strong{Reproducibility (AV)}: Appraiser variation - variability between
operators measuring the same parts
\item \strong{Part-to-Part (PV)}: True variation between parts
}
\subsection{Acceptance Criteria (typical guidelines)}{
\itemize{
\item \%R&R < 10\%: Measurement system acceptable
\item \%R&R 10-30\%: Measurement system may be acceptable depending on application
\item \%R&R > 30\%: Measurement system needs improvement
}

The number of distinct categories (ndc) should be >= 5 for a capable
measurement system.
}
}
\examples{
# Gage R&R study with 10 parts, 3 operators, 2 replicates each
set.seed(123)
data <- expand.grid(
  part = 1:10,
  operator = c("A", "B", "C"),
  replicate = 1:2
)
data$measurement <- 50 +
  (data$part - 5) * 2 +  # Part-to-part variation
  ifelse(data$operator == "A", 0.5,
         ifelse(data$operator == "B", -0.3, 0)) +  # Operator effect
  rnorm(nrow(data), 0, 0.5)  # Repeatability

result <- measure_gage_rr(
  data,
  response_col = "measurement",
  part_col = "part",
  operator_col = "operator",
  tolerance = 20
)
print(result)
}
\seealso{
\code{\link[=measure_repeatability]{measure_repeatability()}}, \code{\link[=measure_intermediate_precision]{measure_intermediate_precision()}}

Other precision: 
\code{\link{measure_intermediate_precision}()},
\code{\link{measure_repeatability}()},
\code{\link{measure_reproducibility}()}
}
\concept{precision}
