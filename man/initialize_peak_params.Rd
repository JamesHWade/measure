% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/peak-deconv-initialize.R
\name{initialize_peak_params}
\alias{initialize_peak_params}
\title{Smart Parameter Initialization for Peak Deconvolution}
\usage{
initialize_peak_params(
  x,
  y,
  n_peaks,
  models,
  peak_indices = NULL,
  smooth = TRUE,
  smooth_span = 0.05
)
}
\arguments{
\item{x}{Numeric vector of x-axis values.}

\item{y}{Numeric vector of y-axis values.}

\item{n_peaks}{Number of peaks to initialize.}

\item{models}{List of \code{peak_model} objects (one per peak).}

\item{peak_indices}{Optional integer vector of peak indices (if already known).}

\item{smooth}{Logical. If \code{TRUE}, smooth data before peak detection.}

\item{smooth_span}{Smoothing span for LOESS (if \code{smooth = TRUE}).}
}
\value{
List of initialized parameter lists (one per peak).
}
\description{
Initializes peak model parameters using actual peak properties from the data
rather than naive guesses, improving optimization convergence.
}
\examples{
# Create synthetic data with two peaks
x <- seq(0, 20, by = 0.1)
y <- 1.5 * exp(-0.5 * ((x - 8) / 1)^2) +
  0.8 * exp(-0.5 * ((x - 12) / 1.5)^2)

models <- list(gaussian_peak_model(), gaussian_peak_model())
init_params <- initialize_peak_params(x, y, n_peaks = 2, models = models)

}
\seealso{
Other peak-deconvolution: 
\code{\link{add_param_jitter}()},
\code{\link{assess_deconv_quality}()},
\code{\link{check_quality_gates}()},
\code{\link{optimize_deconvolution}()}
}
\concept{peak-deconvolution}
