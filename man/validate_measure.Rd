% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/axis-validation.R
\name{validate_measure}
\alias{validate_measure}
\title{Validate measure data}
\usage{
validate_measure(
  x,
  checks = c("monotonic", "duplicates", "missing", "spacing"),
  tolerance = 1e-06,
  action = c("error", "warn", "message")
)
}
\arguments{
\item{x}{A \code{measure_tbl}, \code{measure_list}, or data frame with measure column.}

\item{checks}{Character vector of checks to perform. Default is all checks:
\code{"monotonic"}, \code{"duplicates"}, \code{"missing"}, \code{"spacing"}.}

\item{tolerance}{Numeric tolerance for spacing regularity check.
Default is 1e-6.}

\item{action}{What to do when validation fails: \code{"error"} (default),
\code{"warn"}, or \code{"message"}.}
}
\value{
Invisibly returns a list with validation results. Each element
is a list with \code{valid} (logical), \code{message} (character), and \code{details}.
}
\description{
Performs comprehensive validation checks on measure data, including
axis monotonicity, duplicate detection, missing value detection, and
spacing regularity.
}
\examples{
# Create valid measure data
spec <- new_measure_tbl(location = 1:100, value = sin(1:100 / 10))
validate_measure(spec)

# Data with issues
spec_dup <- new_measure_tbl(location = c(1, 2, 2, 3), value = c(1, 2, 3, 4))
try(validate_measure(spec_dup))

# Only check specific issues
validate_measure(spec, checks = c("monotonic", "missing"))

}
