% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/feature-engineering.R
\name{step_measure_ratios}
\alias{step_measure_ratios}
\title{Calculate Region Ratios}
\usage{
step_measure_ratios(
  recipe,
  numerator,
  denominator,
  name = NULL,
  method = c("trapezoid", "simpson"),
  measures = NULL,
  prefix = "ratio_",
  role = "predictor",
  trained = FALSE,
  skip = FALSE,
  id = recipes::rand_id("measure_ratios")
)
}
\arguments{
\item{recipe}{A recipe object.}

\item{numerator}{A numeric vector of length 2 specifying the numerator region.}

\item{denominator}{A numeric vector of length 2 specifying the denominator region.}

\item{name}{Output column name. If \code{NULL}, auto-generated from prefix.}

\item{method}{Integration method: \code{"trapezoid"} (default) or \code{"simpson"}.}

\item{measures}{An optional character vector of measure column names.}

\item{prefix}{Prefix for output column name if \code{name} is NULL. Default is \code{"ratio_"}.}

\item{role}{Role for generated column. Default is \code{"predictor"}.}

\item{trained}{Logical indicating if the step has been trained.}

\item{skip}{Logical. Should the step be skipped when baking?}

\item{id}{Unique step identifier.}
}
\value{
An updated recipe with the new step added.
}
\description{
\code{step_measure_ratios()} creates a \emph{specification} of a recipe step that
calculates ratios between integrated regions.
}
\details{
This step calculates the ratio of integrated areas between two regions:
\code{ratio = integral(numerator) / integral(denominator)}

This is useful for calculating peak ratios in spectroscopy, or relative
concentrations in chromatography.

If the denominator integral is zero or NA, the ratio will be NA.
}
\examples{
library(recipes)

rec <- recipe(water + fat + protein ~ ., data = meats_long) |>
  update_role(id, new_role = "id") |>
  step_measure_input_long(transmittance, location = vars(channel)) |>
  step_measure_ratios(
    numerator = c(1, 30),
    denominator = c(70, 100),
    name = "low_high_ratio"
  ) |>
  prep()

bake(rec, new_data = NULL)
}
\seealso{
Other measure-features: 
\code{\link{step_measure_bin}()},
\code{\link{step_measure_integrals}()},
\code{\link{step_measure_moments}()}
}
\concept{measure-features}
