% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/detrend.R
\name{step_measure_detrend}
\alias{step_measure_detrend}
\title{Remove Trend from Measurements}
\usage{
step_measure_detrend(
  recipe,
  measures = NULL,
  degree = 1L,
  role = NA,
  trained = FALSE,
  skip = FALSE,
  id = recipes::rand_id("measure_detrend")
)
}
\arguments{
\item{recipe}{A recipe object. The step will be added to the sequence of
operations for this recipe.}

\item{measures}{An optional character vector of measure column names to
process. If \code{NULL} (the default), all measure columns (columns with class
\code{measure_list}) will be processed.}

\item{degree}{Polynomial degree for trend fitting. Default is \code{1} (linear
detrending). Use \code{0} to remove only the mean (centering).}

\item{role}{Not used by this step since no new variables are created.}

\item{trained}{A logical to indicate if the quantities for preprocessing
have been estimated.}

\item{skip}{A logical. Should the step be skipped when the recipe is baked?}

\item{id}{A character string that is unique to this step to identify it.}
}
\value{
An updated version of \code{recipe} with the new step added to the
sequence of any existing operations.
}
\description{
\code{step_measure_detrend()} creates a \emph{specification} of a recipe step that
removes a polynomial trend from measurement data. This is useful for
removing drift, offset, or slowly varying background effects.
}
\details{
Detrending removes a polynomial trend from each spectrum. This is simpler
than baseline correction methods like ALS or robust fitting, but effective
for:
\itemize{
\item \strong{Linear drift} (\code{degree = 1}): Instrumental drift, temperature effects
\item \strong{Offset removal} (\code{degree = 0}): Centers each spectrum at zero mean
\item \strong{Curved trends} (\verb{degree = 2+}): Gradual curvature from scattering
}

Unlike \code{\link[=step_measure_baseline_poly]{step_measure_baseline_poly()}}, detrending fits the polynomial to
ALL points without iterative peak exclusion. This makes it faster and
appropriate when:
\itemize{
\item The trend is the dominant feature (not peaks)
\item You want to preserve peak structure while removing background
\item Processing time-series or process data with drift
}

\strong{No selectors should be supplied to this step function}. The data should be
in the internal format produced by \code{\link[=step_measure_input_wide]{step_measure_input_wide()}} or
\code{\link[=step_measure_input_long]{step_measure_input_long()}}.
}
\section{Tidying}{
When you \code{\link[recipes:tidy.recipe]{tidy()}} this step, a tibble with columns
\code{terms}, \code{degree}, and \code{id} is returned.
}

\examples{
library(recipes)

# Linear detrending
rec <- recipe(water + fat + protein ~ ., data = meats_long) |>
  update_role(id, new_role = "id") |>
  step_measure_input_long(transmittance, location = vars(channel)) |>
  step_measure_detrend(degree = 1) |>
  prep()

bake(rec, new_data = NULL)

# Mean centering only (degree = 0)
rec2 <- recipe(water + fat + protein ~ ., data = meats_long) |>
  update_role(id, new_role = "id") |>
  step_measure_input_long(transmittance, location = vars(channel)) |>
  step_measure_detrend(degree = 0) |>
  prep()
}
\seealso{
\code{\link[=step_measure_baseline_poly]{step_measure_baseline_poly()}} for baseline correction with peak
exclusion.

Other measure-baseline: 
\code{\link{step_measure_baseline_airpls}()},
\code{\link{step_measure_baseline_als}()},
\code{\link{step_measure_baseline_custom}()},
\code{\link{step_measure_baseline_gpc}()},
\code{\link{step_measure_baseline_poly}()},
\code{\link{step_measure_baseline_py}()},
\code{\link{step_measure_baseline_rf}()},
\code{\link{step_measure_baseline_rolling}()},
\code{\link{step_measure_baseline_snip}()}
}
\concept{measure-baseline}
