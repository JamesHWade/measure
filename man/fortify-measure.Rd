% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{fortify-measure}
\alias{fortify-measure}
\alias{fortify.measure_tbl}
\alias{fortify.measure_list}
\title{Convert Measure Objects to Data Frames for Plotting}
\usage{
\method{fortify}{measure_tbl}(model, data = NULL, ...)

\method{fortify}{measure_list}(model, data = NULL, ...)
}
\arguments{
\item{model}{A \code{measure_tbl} or \code{measure_list} object.}

\item{data}{Ignored. Present for compatibility with generic.}

\item{...}{Additional arguments (currently unused).}
}
\value{
A tibble with columns \code{location} and \code{value} (for \code{measure_tbl})
or \code{location}, \code{value}, and \code{sample} (for \code{measure_list}).
}
\description{
These methods convert measure objects to data frames suitable for
use with ggplot2.
}
\examples{
\dontrun{
library(ggplot2)

# Single spectrum
spec <- new_measure_tbl(location = 1:100, value = rnorm(100))
ggplot(fortify(spec), aes(location, value)) + geom_line()

# Multiple spectra (from recipe output)
rec <- recipe(water ~ ., data = meats_long) |>
  step_measure_input_long(transmittance, location = vars(channel)) |>
  prep()
baked <- bake(rec, new_data = NULL)
ggplot(fortify(baked$.measures), aes(location, value, group = sample)) +
  geom_line(alpha = 0.5)
}

}
