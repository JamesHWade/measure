% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/uncertainty.R
\name{uncertainty_component}
\alias{uncertainty_component}
\title{Create an Uncertainty Component}
\usage{
uncertainty_component(
  name,
  value,
  type = c("A", "B"),
  sensitivity = 1,
  df = Inf,
  distribution = c("normal", "rectangular", "triangular", "u-shaped"),
  coverage_factor = 1
)
}
\arguments{
\item{name}{Name/description of the uncertainty source.}

\item{value}{Standard uncertainty value (u).}

\item{type}{Type of evaluation:
\itemize{
\item \code{"A"}: Statistical evaluation (from repeated measurements)
\item \code{"B"}: Evaluated by other means (from specifications, certificates, etc.)
}}

\item{sensitivity}{Sensitivity coefficient (c). Default is 1.
The contribution to combined uncertainty is \verb{|c| * u}.}

\item{df}{Degrees of freedom for this component. Default is \code{Inf}
(for Type B with no DOF information).}

\item{distribution}{Distribution assumed for Type B:
\itemize{
\item \code{"normal"}: Normal distribution (default for Type A)
\item \code{"rectangular"}: Uniform distribution (common for Type B)
\item \code{"triangular"}: Triangular distribution
\item \code{"u-shaped"}: U-shaped distribution
}}

\item{coverage_factor}{Coverage factor (k) used to derive this value from
an expanded uncertainty. Default is 1 (value is already standard uncertainty).}
}
\value{
An \code{uncertainty_component} object.
}
\description{
Defines a single uncertainty component for use in an uncertainty budget.
This follows ISO GUM terminology with Type A (statistical) and Type B
(other means) uncertainty evaluation.
}
\details{
\subsection{Type A Evaluation}{

For Type A components, the standard uncertainty is typically the standard
error of the mean: \code{u = s / sqrt(n)}, with \code{df = n - 1}.
}

\subsection{Type B Evaluation}{

For Type B components from expanded uncertainties with coverage k:
\code{u = U / k}. For rectangular distributions: \code{u = a / sqrt(3)}.
}
}
\examples{
# Type A: Repeatability from 10 measurements
u_repeat <- uncertainty_component(
  name = "Repeatability",
  value = 0.05,  # Standard error of mean
  type = "A",
  df = 9
)

# Type B: Calibrator uncertainty from certificate (k=2)
u_cal <- uncertainty_component(
  name = "Calibrator",
  value = 0.02 / 2,  # Divide expanded uncertainty by k
  type = "B",
  df = 50
)

# Type B: Temperature effect (rectangular distribution)
u_temp <- uncertainty_component(
  name = "Temperature",
  value = 0.1 / sqrt(3),  # Half-width / sqrt(3) for rectangular
  type = "B",
  distribution = "rectangular"
)

}
\seealso{
\code{\link[=measure_uncertainty_budget]{measure_uncertainty_budget()}} for combining components,
\code{\link[=measure_uncertainty]{measure_uncertainty()}} for quick uncertainty calculation.
}
