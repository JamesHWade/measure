% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/criteria.R
\name{measure_assess}
\alias{measure_assess}
\title{Assess Data Against Acceptance Criteria}
\usage{
measure_assess(data, criteria, action = c("return", "warn", "error"))
}
\arguments{
\item{data}{A named list or data frame containing the values to assess.
Names must match criterion names.}

\item{criteria}{A \code{\link[=measure_criteria]{measure_criteria()}} object defining the acceptance criteria.}

\item{action}{What to do on failure: \code{"return"} (default) returns the
assessment table, \code{"warn"} issues a warning for failures,
\code{"error"} stops on any critical failures.}
}
\value{
A tibble with class \code{measure_assessment} containing:
\itemize{
\item \code{criterion}: Name of the criterion
\item \code{value}: The observed value
\item \code{threshold}: The threshold value(s)
\item \code{operator}: The comparison operator
\item \code{pass}: Logical indicating pass/fail
\item \code{priority}: Priority level of the criterion
\item \code{description}: Human-readable description
}
}
\description{
Evaluates a set of values against acceptance criteria and returns a
detailed assessment table with pass/fail status.
}
\examples{
# Define criteria
crit <- measure_criteria(
  cv_qc = list("<", 15),
  r_squared = list(">=", 0.99),
  recovery = list("between", c(80, 120))
)

# Assess results
results <- list(cv_qc = 12.5, r_squared = 0.995, recovery = 98.2)
measure_assess(results, crit)

# Assess with some failures
results_bad <- list(cv_qc = 18.3, r_squared = 0.985, recovery = 75)
measure_assess(results_bad, crit)

}
\seealso{
\code{\link[=measure_criteria]{measure_criteria()}} for creating criteria,
\code{\link[=criterion]{criterion()}} for individual criteria.
}
