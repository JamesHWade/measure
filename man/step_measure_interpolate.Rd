% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/region-operations.R
\name{step_measure_interpolate}
\alias{step_measure_interpolate}
\title{Interpolate Gaps in Measurement Data}
\usage{
step_measure_interpolate(
  recipe,
  ranges,
  method = c("linear", "spline"),
  measures = NULL,
  role = NA,
  trained = FALSE,
  skip = FALSE,
  id = recipes::rand_id("measure_interpolate")
)
}
\arguments{
\item{recipe}{A recipe object.}

\item{ranges}{A list of numeric vectors specifying ranges to interpolate.
Each element should be a vector of length 2: \code{c(min, max)}.}

\item{method}{Interpolation method: "linear" or "spline". Default is "linear".}

\item{measures}{An optional character vector of measure column names.}

\item{role}{Not used.}

\item{trained}{Logical indicating if the step has been trained.}

\item{skip}{Logical. Should the step be skipped when baking?}

\item{id}{Unique step identifier.}
}
\value{
An updated recipe with the new step added.
}
\description{
\code{step_measure_interpolate()} creates a \emph{specification} of a recipe step that
fills gaps or missing values in measurement data using interpolation.
}
\details{
This step is useful for:
\itemize{
\item Filling gaps left by excluded regions that need restoration
\item Handling missing or invalid data points
\item Smoothing over detector saturation regions
}

The interpolation uses data points immediately outside the specified
ranges to estimate values within the ranges.
}
\examples{
library(recipes)

# Interpolate over a problematic region
rec <- recipe(water + fat + protein ~ ., data = meats_long) |>
  update_role(id, new_role = "id") |>
  step_measure_input_long(transmittance, location = vars(channel)) |>
  step_measure_interpolate(ranges = list(c(40, 50)), method = "spline") |>
  prep()
}
\concept{measure-region}
