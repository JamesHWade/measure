% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/criteria.R
\name{criterion}
\alias{criterion}
\title{Create an Acceptance Criterion}
\usage{
criterion(
  name,
  operator = c("<", "<=", ">", ">=", "==", "!=", "between", "outside"),
  threshold,
  description = NULL,
  priority = c("major", "critical", "minor")
)
}
\arguments{
\item{name}{Character string naming this criterion (e.g., "cv_qc", "r_squared").}

\item{operator}{Comparison operator: \code{"<"}, \code{"<="}, \code{">"}, \code{">="}, \code{"=="},
\code{"!="}, \code{"between"}, or \code{"outside"}.}

\item{threshold}{Numeric threshold value. For \code{"between"} and \code{"outside"},
provide a length-2 vector \code{c(lower, upper)}.}

\item{description}{Optional human-readable description of the criterion.}

\item{priority}{Optional priority level: \code{"critical"}, \code{"major"}, \code{"minor"}.
Affects how failures are reported.}
}
\value{
A \code{measure_criterion} object.
}
\description{
Defines a single acceptance criterion for analytical validation. Criteria
are used with \code{\link[=measure_assess]{measure_assess()}} to produce pass/fail decisions.
}
\examples{
# QC coefficient of variation must be < 15\%
criterion("cv_qc", "<", 15, description = "QC CV < 15\%")

# R-squared must be >= 0.99
criterion("r_squared", ">=", 0.99)

# Recovery must be between 80\% and 120\%
criterion("recovery", "between", c(80, 120), priority = "critical")

}
\seealso{
\code{\link[=measure_criteria]{measure_criteria()}} for combining multiple criteria,
\code{\link[=measure_assess]{measure_assess()}} for evaluating criteria.
}
