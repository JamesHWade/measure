% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/peak-algorithm-registry.R
\name{register_peak_algorithm}
\alias{register_peak_algorithm}
\title{Register a Peak Detection Algorithm}
\usage{
register_peak_algorithm(
  name,
  algorithm_fn,
  pack_name,
  description = "",
  default_params = list(),
  param_info = list(),
  technique = NULL
)
}
\arguments{
\item{name}{Algorithm name (e.g., \code{"cwt"}, \code{"finderskeepers"}). Must be unique.}

\item{algorithm_fn}{The algorithm function. Must accept \code{location}, \code{value},
and return a \code{peaks_tbl} object. Additional parameters are passed via \code{...}.}

\item{pack_name}{Source package name. Use \code{pkgname} from \code{.onLoad()}.}

\item{description}{Brief description of the algorithm.}

\item{default_params}{Named list of default parameter values.}

\item{param_info}{Named list of parameter descriptions (for documentation).}

\item{technique}{Optional technique name (e.g., \code{"SEC/GPC"}). If \code{NULL},
algorithm is considered general-purpose.}
}
\value{
Invisible \code{TRUE}.
}
\description{
Registers a peak detection algorithm with the measure package. This function
can be called from technique pack packages to add specialized algorithms.
}
\examples{
\dontrun{
# In a technique pack's R/zzz.R file:
.onLoad <- function(libname, pkgname) {
  if (requireNamespace("measure", quietly = TRUE)) {
    measure::register_peak_algorithm(
      name = "sec_loess_ist",
      algorithm_fn = .detect_peaks_sec_loess_ist,
      pack_name = pkgname,
      description = "LOESS smoothing with iterative soft thresholding",
      default_params = list(loess_span = 0.01, ist_points = 50),
      technique = "SEC/GPC"
    )
  }
}
}

}
\seealso{
\code{\link[=peak_algorithms]{peak_algorithms()}}, \code{\link[=get_peak_algorithm]{get_peak_algorithm()}}
}
