% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/peak-deconv-quality.R
\name{assess_deconv_quality}
\alias{assess_deconv_quality}
\title{Assess Deconvolution Quality}
\usage{
assess_deconv_quality(x, y, result, models)
}
\arguments{
\item{x}{Numeric vector of x-axis values.}

\item{y}{Numeric vector of observed y-axis values.}

\item{result}{Deconvolution result list from \code{optimize_deconvolution()}.}

\item{models}{List of \code{peak_model} objects used in deconvolution.}
}
\value{
A list of class \code{deconv_quality} containing:
\itemize{
\item \code{goodness_of_fit}: R-squared, RMSE, MAE, chi-squared
\item \code{information_criteria}: AIC, BIC, AICc
\item \code{peak_quality}: Per-peak purity, overlap, area
\item \code{residual_analysis}: Autocorrelation, heteroscedasticity, normality tests
\item \code{overall_grade}: Letter grade (A/B/C/D/F)
\item \code{convergence_info}: Optimization convergence details
}
}
\description{
Calculates comprehensive quality metrics for a peak deconvolution fit,
including goodness-of-fit statistics, information criteria, per-peak
quality, and residual diagnostics.
}
\examples{
# Create synthetic data and fit
x <- seq(0, 20, by = 0.1)
true_y <- 1.5 * exp(-0.5 * ((x - 8) / 1)^2) +
  0.8 * exp(-0.5 * ((x - 12) / 1.5)^2)
y <- true_y + rnorm(length(x), sd = 0.05)

models <- list(gaussian_peak_model(), gaussian_peak_model())
init_params <- list(
  list(height = 1.2, center = 7.5, width = 1.2),
  list(height = 0.6, center = 12.5, width = 1.8)
)

result <- optimize_deconvolution(x, y, models, init_params)
quality <- assess_deconv_quality(x, y, result, models)
print(quality)

}
\seealso{
Other peak-deconvolution: 
\code{\link{add_param_jitter}()},
\code{\link{check_quality_gates}()},
\code{\link{initialize_peak_params}()},
\code{\link{optimize_deconvolution}()}
}
\concept{peak-deconvolution}
