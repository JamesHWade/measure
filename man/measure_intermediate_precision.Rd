% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/precision.R
\name{measure_intermediate_precision}
\alias{measure_intermediate_precision}
\title{Intermediate Precision (Between-Run Precision)}
\usage{
measure_intermediate_precision(
  data,
  response_col,
  factors,
  group_col = NULL,
  conf_level = 0.95
)
}
\arguments{
\item{data}{A data frame containing measurements with factor columns.}

\item{response_col}{Name of the column containing the response values.}

\item{factors}{Character vector of factor column names (e.g., \code{c("day", "analyst")}).}

\item{group_col}{Optional grouping column (e.g., concentration level).}

\item{conf_level}{Confidence level for intervals. Default is 0.95.}
}
\value{
A \code{measure_precision} object containing variance components and
precision estimates:
\itemize{
\item \code{component}: Name of the variance component
\item \code{variance}: Estimated variance
\item \code{percent_variance}: Percentage of total variance
\item \code{sd}: Standard deviation (square root of variance)
\item \code{cv}: Coefficient of variation (\%) for that component
}
}
\description{
Calculates intermediate precision statistics for measurements performed
under varying conditions (different days, analysts, or instruments).
}
\details{
Intermediate precision quantifies the variability due to different
conditions within the same laboratory. This typically includes:
\itemize{
\item Different days
\item Different analysts
\item Different equipment (of the same type)
}

The function uses a one-way or nested ANOVA approach to estimate
variance components. For more complex designs, consider using mixed
effects models with the \code{lme4} package.
}
\examples{
# Intermediate precision across days
set.seed(123)
data <- data.frame(
  day = rep(1:5, each = 6),
  concentration = rnorm(30, mean = 100, sd = 3) +
    rep(rnorm(5, 0, 2), each = 6)  # Day effect
)
measure_intermediate_precision(data, "concentration", factors = "day")
}
\seealso{
\code{\link[=measure_repeatability]{measure_repeatability()}}, \code{\link[=measure_reproducibility]{measure_reproducibility()}}

Other precision: 
\code{\link{measure_gage_rr}()},
\code{\link{measure_repeatability}()},
\code{\link{measure_reproducibility}()}
}
\concept{precision}
