% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/control-limits.R
\name{measure_system_suitability}
\alias{measure_system_suitability}
\title{System Suitability Check}
\usage{
measure_system_suitability(
  data,
  metrics,
  sample_type_col = NULL,
  sst_type = "sst"
)
}
\arguments{
\item{data}{A data frame containing system suitability data.}

\item{metrics}{Named list of columns and their acceptance criteria.
Each element should be a list with \code{col}, \code{min}, and/or \code{max}.}

\item{sample_type_col}{Optional column identifying sample types.}

\item{sst_type}{Value in sample_type_col that identifies SST samples.}
}
\value{
A \code{measure_sst} object containing:
\itemize{
\item \code{results}: Pass/fail status for each metric
\item \code{summary}: Overall pass/fail and summary statistics
\item \code{details}: Individual sample results
}
}
\description{
Performs system suitability tests on QC or reference samples to verify
instrument performance meets requirements.
}
\details{
System suitability testing (SST) verifies that the analytical system
is performing adequately before, during, or after a run. Common metrics
include:
\itemize{
\item Peak resolution
\item Retention time reproducibility
\item Peak symmetry/tailing factor
\item Signal-to-noise ratio
\item Plate count
}
}
\examples{
# System suitability check
sst_data <- data.frame(
  sample_id = paste0("SST_", 1:5),
  resolution = c(2.1, 2.3, 2.2, 2.0, 2.1),
  tailing = c(1.1, 1.0, 1.2, 1.1, 1.0),
  plates = c(5200, 5100, 5300, 5000, 5150)
)

result <- measure_system_suitability(
  sst_data,
  metrics = list(
    resolution = list(col = "resolution", min = 2.0),
    tailing = list(col = "tailing", max = 1.5),
    plates = list(col = "plates", min = 5000)
  )
)
print(result)
}
\seealso{
Other control-charts: 
\code{\link{measure_control_chart}()},
\code{\link{measure_control_limits}()}
}
\concept{control-charts}
