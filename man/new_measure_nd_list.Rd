% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nd_class.R
\name{new_measure_nd_list}
\alias{new_measure_nd_list}
\title{Create a new n-dimensional measure list}
\usage{
new_measure_nd_list(x = list())
}
\arguments{
\item{x}{A list of \code{measure_nd_tbl} objects or tibbles with \verb{location_*}
and \code{value} columns.}
}
\value{
A list with class \code{measure_nd_list}.
}
\description{
Constructor for creating a collection of n-dimensional measurements
suitable for use as a list column in a data frame. Each element should be
a \code{measure_nd_tbl} or tibble with \verb{location_*} and \code{value} columns.
}
\examples{
# Create individual 2D measurements
meas1 <- new_measure_nd_tbl(
  location_1 = rep(1:5, each = 3),
  location_2 = rep(1:3, times = 5),
  value = rnorm(15)
)
meas2 <- new_measure_nd_tbl(
  location_1 = rep(1:5, each = 3),
  location_2 = rep(1:3, times = 5),
  value = rnorm(15)
)

# Combine into a measure_nd_list
meas_list <- new_measure_nd_list(list(meas1, meas2))
meas_list

}
\seealso{
\code{\link[=new_measure_nd_tbl]{new_measure_nd_tbl()}} for creating individual nD measurements,
\code{\link[=is_measure_nd_list]{is_measure_nd_list()}} for checking object class.
}
