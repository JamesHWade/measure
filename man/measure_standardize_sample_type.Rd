% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metadata-validation.R
\name{measure_standardize_sample_type}
\alias{measure_standardize_sample_type}
\title{Standardize Sample Type Values}
\usage{
measure_standardize_sample_type(
  data,
  col = "sample_type",
  mapping = NULL,
  unknown_action = c("error", "warn", "keep", "unknown")
)
}
\arguments{
\item{data}{A data frame containing a sample type column.}

\item{col}{Name of the sample type column. Default is \code{"sample_type"}.}

\item{mapping}{A named list mapping canonical types to vectors of aliases.
For example: \code{list(qc = c("QC", "quality_control", "pooled_qc"))}.
If NULL, uses default case-insensitive matching.}

\item{unknown_action}{What to do with values that don't match any mapping:
\itemize{
\item \code{"error"} (default): Stop with error
\item \code{"warn"}: Warn and keep original value
\item \code{"keep"}: Silently keep original value
\item \code{"unknown"}: Convert to "unknown"
}}
}
\value{
The data frame with standardized sample_type values.
}
\description{
Converts non-standard sample type values to canonical form using a
user-specified mapping. This is useful when data uses different
naming conventions (e.g., "QC", "quality_control", "pooled_qc").
}
\examples{
# Data with non-standard sample types
data <- data.frame(
  sample_id = 1:5,
  sample_type = c("QC", "STD", "BLK", "UNK", "REF")
)

# Standardize with custom mapping
measure_standardize_sample_type(
  data,
  mapping = list(
    qc = c("QC", "qc", "quality_control"),
    standard = c("STD", "std", "cal"),
    blank = c("BLK", "blk", "blank"),
    unknown = c("UNK", "unk", "sample"),
    reference = c("REF", "ref")
  )
)
}
