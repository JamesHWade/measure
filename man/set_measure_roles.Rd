% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data-organization.R
\name{set_measure_roles}
\alias{set_measure_roles}
\title{Set Measure Roles in a Recipe}
\usage{
set_measure_roles(
  recipe,
  id_cols = NULL,
  blank_cols = NULL,
  qc_cols = NULL,
  standard_cols = NULL,
  metadata_cols = NULL,
  measure_cols = NULL
)
}
\arguments{
\item{recipe}{A recipe object.}

\item{id_cols}{Column(s) to assign "id" role. Accepts tidyselect.}

\item{blank_cols}{Column(s) to assign "blank" role. Accepts tidyselect.}

\item{qc_cols}{Column(s) to assign "qc" role. Accepts tidyselect.}

\item{standard_cols}{Column(s) to assign "standard" role. Accepts tidyselect.}

\item{metadata_cols}{Column(s) to assign "metadata" role. Accepts tidyselect.}

\item{measure_cols}{Column(s) to assign "measure" role. Accepts tidyselect.}
}
\value{
Updated recipe object with roles assigned.
}
\description{
Batch assign roles to columns based on their detected types or
explicit patterns. This is a convenience wrapper around
\code{\link[recipes:roles]{recipes::update_role()}} for common analytical data patterns.
}
\details{
Common roles for analytical chemistry workflows:\tabular{ll}{
   Role \tab Purpose \cr
   id \tab Sample identifiers (not used in modeling) \cr
   blank \tab Blank/background samples for subtraction \cr
   qc \tab Quality control samples \cr
   standard \tab Calibration standards \cr
   metadata \tab Sample metadata (not used in modeling) \cr
   measure \tab Measurement columns for input steps \cr
   predictor \tab Columns used as model predictors \cr
   outcome \tab Target variable(s) for modeling \cr
}
}
\examples{
\dontrun{
library(recipes)

# Basic role assignment
rec <- recipe(outcome ~ ., data = my_data) |>
  set_measure_roles(
    id_cols = sample_id,
    metadata_cols = c(batch, operator)
  )

# With QC and blank identification by column name patterns
rec <- recipe(outcome ~ ., data = my_data) |>
  set_measure_roles(
    id_cols = sample_id,
    blank_cols = starts_with("blank_"),
    qc_cols = starts_with("qc_")
  )
}

}
