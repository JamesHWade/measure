% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/uncertainty.R
\name{measure_uncertainty_budget}
\alias{measure_uncertainty_budget}
\title{Create an Uncertainty Budget}
\usage{
measure_uncertainty_budget(..., .list = NULL, k = 2, result_value = NULL)
}
\arguments{
\item{...}{\code{\link[=uncertainty_component]{uncertainty_component()}} objects to include in the budget.}

\item{.list}{Optional list of uncertainty components.}

\item{k}{Coverage factor for expanded uncertainty. Default is 2
(approximately 95\% coverage for normal distribution).}

\item{result_value}{Optional. The measurement result value, used for
calculating relative uncertainty.}
}
\value{
A \code{measure_uncertainty_budget} object containing:
\itemize{
\item \code{components}: List of input uncertainty components
\item \code{combined_u}: Combined standard uncertainty
\item \code{effective_df}: Effective degrees of freedom (Welch-Satterthwaite)
\item \code{coverage_factor}: The k value used
\item \code{expanded_U}: Expanded uncertainty (k * combined_u)
\item \code{result_value}: The measurement result (if provided)
\item \code{relative_u}: Relative standard uncertainty (if result provided)
}
}
\description{
Combines multiple uncertainty components into a complete uncertainty
budget following ISO GUM methodology. Calculates combined standard
uncertainty, effective degrees of freedom (Welch-Satterthwaite), and
expanded uncertainty.
}
\details{
\subsection{Combined Standard Uncertainty}{

Calculated as the root sum of squares of contributions:
\deqn{u_c = \sqrt{\sum_i (c_i \cdot u_i)^2}}
}

\subsection{Welch-Satterthwaite Effective Degrees of Freedom}{

\deqn{\nu_{eff} = \frac{u_c^4}{\sum_i \frac{(c_i \cdot u_i)^4}{\nu_i}}}

This is used to determine the appropriate coverage factor for a
given confidence level.
}

\subsection{Expanded Uncertainty}{

\deqn{U = k \cdot u_c}

With k=2, this provides approximately 95\% coverage.
}
}
\examples{
# Create components
u_repeat <- uncertainty_component("Repeatability", 0.05, type = "A", df = 9)
u_cal <- uncertainty_component("Calibrator", 0.02, type = "B", df = 50)
u_temp <- uncertainty_component("Temperature", 0.03, type = "B")

# Create budget
budget <- measure_uncertainty_budget(u_repeat, u_cal, u_temp, k = 2)
print(budget)

# With result value for relative uncertainty
budget <- measure_uncertainty_budget(
  u_repeat, u_cal, u_temp,
  result_value = 10.5
)

}
\seealso{
\code{\link[=uncertainty_component]{uncertainty_component()}} for creating components,
\code{\link[=tidy.measure_uncertainty_budget]{tidy.measure_uncertainty_budget()}} for extracting results,
\code{\link[=autoplot.measure_uncertainty_budget]{autoplot.measure_uncertainty_budget()}} for visualization.
}
