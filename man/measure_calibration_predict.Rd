% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibration-fit.R
\name{measure_calibration_predict}
\alias{measure_calibration_predict}
\title{Predict Concentrations from Calibration Curve}
\usage{
measure_calibration_predict(
  object,
  newdata,
  interval = c("none", "confidence", "prediction"),
  level = 0.95,
  ...
)
}
\arguments{
\item{object}{A \link{measure_calibration} object from \code{\link[=measure_calibration_fit]{measure_calibration_fit()}}.}

\item{newdata}{A data frame containing the response values to predict from.
Must contain a column with the same name as the response variable in the
calibration formula.}

\item{interval}{Type of interval to calculate:
\itemize{
\item \code{"none"} (default): Point estimates only
\item \code{"confidence"}: Confidence intervals
\item \code{"prediction"}: Prediction intervals
}}

\item{level}{Confidence level for intervals (default 0.95).}

\item{...}{Additional arguments (unused).}
}
\value{
A tibble with columns:
\itemize{
\item \code{.pred_conc}: Predicted concentration
\item \code{.pred_lower}: Lower bound (if intervals requested)
\item \code{.pred_upper}: Upper bound (if intervals requested)
}
}
\description{
Uses a fitted calibration curve to predict concentrations from responses.
}
\details{
For inverse prediction (response -> concentration), the function uses
root-finding when the model is quadratic. For linear models, direct
algebraic inversion is used.
\subsection{Interval Calculation}{

Intervals are calculated using the delta method for the inverse prediction.
For quadratic models, intervals are approximate.
}
}
\examples{
# Fit calibration curve
cal_data <- data.frame(
  nominal_conc = c(0, 10, 25, 50, 100),
  response = c(0.5, 15.2, 35.8, 72.1, 148.3)
)
cal <- measure_calibration_fit(cal_data, response ~ nominal_conc)

# Predict concentrations from new responses
unknowns <- data.frame(response = c(45, 85, 120))
measure_calibration_predict(cal, unknowns)

# With prediction intervals
measure_calibration_predict(cal, unknowns, interval = "prediction")

}
\seealso{
\code{\link[=measure_calibration_fit]{measure_calibration_fit()}} for fitting calibration curves.
}
