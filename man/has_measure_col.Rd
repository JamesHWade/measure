% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/measure-class.R
\name{has_measure_col}
\alias{has_measure_col}
\title{Check if data frame has measure column(s)}
\usage{
has_measure_col(data)
}
\arguments{
\item{data}{A data frame.}
}
\value{
Invisibly returns the names of measure columns found.
Throws an error if no measure columns are found.
}
\description{
Checks whether a data frame contains at least one measure column.
This is the recommended way to validate data in step functions.
}
\examples{
library(recipes)

rec <- recipe(water + fat + protein ~ ., data = meats_long) |>
  update_role(id, new_role = "id") |>
  step_measure_input_long(transmittance, location = vars(channel)) |>
  prep()

result <- bake(rec, new_data = NULL)
has_measure_col(result)  # TRUE (returns invisibly)

}
