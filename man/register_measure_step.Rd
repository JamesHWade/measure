% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/registry.R
\name{register_measure_step}
\alias{register_measure_step}
\title{Register a Step from a Technique Pack}
\usage{
register_measure_step(
  step_name,
  pack_name,
  category = "processing",
  description = "",
  technique = NULL
)
}
\arguments{
\item{step_name}{Full step function name (e.g., \code{"step_sec_mw_averages"}).}

\item{pack_name}{Source package name. Use \code{pkgname} from \code{.onLoad()}.}

\item{category}{Step category (e.g., \code{"preprocessing"}, \code{"calculation"}).}

\item{description}{Brief description of what the step does.}

\item{technique}{Technique name. If \code{NULL}, inherits from the registered pack.}
}
\value{
Invisible \code{TRUE}.
}
\description{
Registers a recipe step with the measure package. This function should be
called from the \code{.onLoad()} function of technique pack packages after
registering the pack with \code{\link[=register_measure_pack]{register_measure_pack()}}.
}
\details{
Registration is idempotent: calling this function multiple times with the
same \code{pack_name} and \code{step_name} will update rather than duplicate the entry.
}
\examples{
\dontrun{
# In a technique pack's R/zzz.R file:
measure::register_measure_step(
  step_name = "step_sec_mw_averages",
  pack_name = pkgname,
  category = "calculation",
  description = "Calculate Mn, Mw, Mz, dispersity"
)
}

}
\seealso{
\code{\link[=register_measure_pack]{register_measure_pack()}}, \code{\link[=measure_steps]{measure_steps()}}
}
