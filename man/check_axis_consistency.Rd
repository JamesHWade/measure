% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/axis-validation.R
\name{check_axis_consistency}
\alias{check_axis_consistency}
\title{Check axis consistency across samples}
\usage{
check_axis_consistency(
  x,
  tolerance = 1e-10,
  action = c("error", "warn", "message")
)
}
\arguments{
\item{x}{A \code{measure_list} or data frame with measure column.}

\item{tolerance}{Numeric tolerance for location comparison. Default is 1e-10.}

\item{action}{What to do when validation fails: \code{"error"} (default),
\code{"warn"}, or \code{"message"}.}
}
\value{
Invisibly returns a list with:
\itemize{
\item \code{consistent}: Logical indicating if axes are consistent
\item \code{reference_locations}: The reference locations (from first sample)
\item \code{inconsistent_samples}: Indices of samples with different axes
\item \code{max_deviation}: Maximum deviation from reference locations
}
}
\description{
Validates that all samples in a \code{measure_list} have consistent axes
(same locations). This is important for matrix operations that assume
aligned data.
}
\examples{
# Consistent axes
specs <- new_measure_list(list(
  new_measure_tbl(location = 1:10, value = rnorm(10)),
  new_measure_tbl(location = 1:10, value = rnorm(10))
))
check_axis_consistency(specs)

# Inconsistent axes
specs_bad <- new_measure_list(list(
  new_measure_tbl(location = 1:10, value = rnorm(10)),
  new_measure_tbl(location = 1:11, value = rnorm(11))
))
try(check_axis_consistency(specs_bad))

}
