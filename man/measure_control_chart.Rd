% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/control-limits.R
\name{measure_control_chart}
\alias{measure_control_chart}
\title{Generate Control Chart}
\usage{
measure_control_chart(
  data,
  response_col,
  order_col,
  limits = NULL,
  rules = c("1_3s", "2_2s", "R_4s", "4_1s", "10x"),
  group_col = NULL
)
}
\arguments{
\item{data}{A data frame containing QC measurements.}

\item{response_col}{Name of the column containing QC values.}

\item{order_col}{Name of the column containing run order/sequence.}

\item{limits}{Optional \code{measure_control_limits} object. If NULL, calculated
from the data.}

\item{rules}{Character vector of Westgard rules to apply. Default is
\code{c("1_3s", "2_2s", "R_4s", "4_1s", "10x")}.}

\item{group_col}{Optional grouping column.}
}
\value{
A \code{measure_control_chart} object containing:
\itemize{
\item \code{data}: The input data with added violation flags
\item \code{limits}: The control limits used
\item \code{violations}: Summary of rule violations
\item \code{rules_applied}: Which rules were checked
}
}
\description{
Creates a control chart with optional multi-rule (Westgard) violation detection.
}
\details{
\subsection{Westgard Rules}{

The function supports common Westgard multi-rules:
\itemize{
\item \strong{1:3s}: One point beyond 3 sigma (action required)
\item \strong{2:2s}: Two consecutive points beyond 2 sigma (warning)
\item \strong{R:4s}: Range of two consecutive points > 4 sigma
\item \strong{4:1s}: Four consecutive points beyond 1 sigma (same side)
\item \strong{10x}: Ten consecutive points on same side of mean
}
}

\subsection{Interpretation}{
\itemize{
\item Violations are flagged with the specific rule that was triggered
\item Multiple rules can be triggered by the same point
\item A run is considered "in control" if no violations are detected
}
}
}
\examples{
# Generate control chart with Westgard rules
set.seed(123)
qc_data <- data.frame(
  run_order = 1:50,
  qc_value = c(rnorm(45, 100, 2), rnorm(5, 106, 2))  # Last 5 shifted
)
chart <- measure_control_chart(qc_data, "qc_value", "run_order")
print(chart)
}
\seealso{
\code{\link[=measure_control_limits]{measure_control_limits()}}, \code{\link[=autoplot.measure_control_chart]{autoplot.measure_control_chart()}}

Other control-charts: 
\code{\link{measure_control_limits}()},
\code{\link{measure_system_suitability}()}
}
\concept{control-charts}
