% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data-organization.R
\name{measure_identify_columns}
\alias{measure_identify_columns}
\title{Identify Column Types in Analytical Data}
\usage{
measure_identify_columns(data, patterns = measure_column_patterns)
}
\arguments{
\item{data}{A data frame to analyze.}

\item{patterns}{Named list of regex patterns for column detection.
Defaults to \code{measure_column_patterns}. Custom patterns can be
provided as a named list where names become the detected type.}
}
\value{
A tibble with columns:
\describe{
\item{column}{Column name}
\item{type}{Detected type (from pattern names, or "other" if no match)}
\item{suggested_role}{Suggested recipe role based on type}
\item{n_values}{Number of non-NA values}
\item{class}{R class of the column}
}
}
\description{
Automatically detects column types in a data frame based on naming
conventions common in analytical chemistry. This helps set up recipes
with appropriate roles for different column types.
}
\details{
Column type detection uses the following naming conventions:\tabular{llll}{
   Prefix \tab Type \tab Suggested Role \tab Use Case \cr
   \verb{wn_*} \tab wavenumber \tab predictor \tab IR spectroscopy (cm^-1) \cr
   \verb{nm_*} \tab wavelength \tab predictor \tab UV-Vis, NIR spectroscopy \cr
   \verb{rt_*} \tab retention_time \tab predictor \tab Chromatography \cr
   \verb{mz_*} \tab mz \tab predictor \tab Mass spectrometry \cr
   \verb{ppm_*} \tab ppm \tab predictor \tab NMR spectroscopy \cr
   \verb{ch_*} \tab channel \tab predictor \tab Generic channel data \cr
   \verb{x_*} \tab generic \tab predictor \tab Generic measurements \cr
}


Columns not matching any pattern are classified as "other" and
suggested as either "outcome" (if numeric), "id" (if character/factor
with unique values), or "predictor".
}
\examples{
# Wide format spectral data
df <- data.frame(
  sample_id = 1:5,
  outcome = rnorm(5),
  wn_1000 = rnorm(5),
  wn_1001 = rnorm(5),
  wn_1002 = rnorm(5)
)
measure_identify_columns(df)

# Chromatography data
df2 <- data.frame(
  id = letters[1:3],
  concentration = c(1.2, 2.3, 3.4),
  rt_0.5 = rnorm(3),
  rt_1.0 = rnorm(3),
  rt_1.5 = rnorm(3)
)
measure_identify_columns(df2)

}
