% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibration-fit.R
\name{measure_calibration_verify}
\alias{measure_calibration_verify}
\title{Verify Calibration Curve Performance}
\usage{
measure_calibration_verify(
  calibration,
  verification_data,
  nominal_col = "nominal_conc",
  acceptance_pct = 15,
  acceptance_pct_lloq = 20,
  lloq = NULL,
  sample_type_col = NULL,
  criteria = NULL
)
}
\arguments{
\item{calibration}{A \link{measure_calibration} object from
\code{\link[=measure_calibration_fit]{measure_calibration_fit()}}.}

\item{verification_data}{A data frame containing verification samples with
known concentrations.}

\item{nominal_col}{Name of the column containing nominal (known)
concentrations. Default is \code{"nominal_conc"}.}

\item{acceptance_pct}{Acceptance criterion as percent deviation from nominal.
Default is 15 (i.e., ±15\%).}

\item{acceptance_pct_lloq}{Acceptance criterion for samples at the lower
limit of quantitation (LLOQ). Default is 20 (i.e., ±20\%).}

\item{lloq}{Lower limit of quantitation. Samples at or near this level use
\code{acceptance_pct_lloq}. Default is NULL (use same criterion for all).}

\item{sample_type_col}{Optional column indicating sample types. Only samples
with type containing "qc" or "ccv" will be used if specified.}

\item{criteria}{Optional \link{measure_criteria} object for custom acceptance
criteria. If provided, overrides \code{acceptance_pct} settings.}
}
\value{
A \code{measure_calibration_verify} object (a tibble) containing:
\itemize{
\item Predicted concentrations
\item Accuracy (\%nominal)
\item Deviation from nominal (\%)
\item Pass/fail status for each sample
\item Overall verification status
}
}
\description{
Evaluates the performance of a calibration curve using verification samples
(continuing calibration verification - CCV, or independent QC samples).
This function assesses whether the calibration remains valid during or
between analytical runs.
}
\details{
\subsection{Verification Workflow}{

Calibration verification is typically performed:
\enumerate{
\item At the beginning and end of analytical batches
\item After every N unknown samples (e.g., every 10)
\item When instrument performance is in question
}
}

\subsection{Acceptance Criteria}{

Default criteria are based on bioanalytical guidelines:
\itemize{
\item Standard samples: ±15\% of nominal
\item LLOQ samples: ±20\% of nominal
}

For more stringent applications (e.g., clinical chemistry), consider
using ±10\% or providing custom criteria.
}
}
\examples{
# Fit calibration
cal_data <- data.frame(
  nominal_conc = c(1, 5, 10, 50, 100, 500),
  response = c(1.2, 5.8, 11.3, 52.1, 105.2, 498.7)
)
cal <- measure_calibration_fit(cal_data, response ~ nominal_conc)

# Verify with QC samples
qc_data <- data.frame(
  sample_id = c("QC_Low", "QC_Mid", "QC_High"),
  nominal_conc = c(3, 75, 400),
  response = c(3.3, 77.2, 385.1)
)

verify_result <- measure_calibration_verify(cal, qc_data)
print(verify_result)

}
\seealso{
\code{\link[=measure_calibration_fit]{measure_calibration_fit()}} for fitting calibration curves,
\code{\link[=measure_calibration_predict]{measure_calibration_predict()}} for prediction,
\code{\link[=measure_criteria]{measure_criteria()}} for custom acceptance criteria.
}
