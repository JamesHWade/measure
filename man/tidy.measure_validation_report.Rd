% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation-report.R
\name{tidy.measure_validation_report}
\alias{tidy.measure_validation_report}
\title{Tidy a Validation Report}
\usage{
\method{tidy}{measure_validation_report}(x, ...)
}
\arguments{
\item{x}{A \code{measure_validation_report} object.}

\item{...}{Additional arguments (currently ignored).}
}
\value{
A tibble with columns:
\itemize{
\item \code{section}: Section name
\item \code{parameter}: Parameter name
\item \code{value}: Parameter value
\item \code{unit}: Unit of measurement (if available)
\item \code{status}: Pass/fail status (if available)
}

Returns an empty tibble if no sections contain tidy-able data.
}
\description{
Extracts key parameters and statistics from all validation sections
into a tidy tibble format suitable for further analysis or reporting.
}
\examples{
# Create sample data
blank_data <- data.frame(
  response = rnorm(10, mean = 50, sd = 15),
  sample_type = "blank"
)
lod_result <- measure_lod(blank_data, response_col = "response")

report <- measure_validation_report(
  title = "Test Report",
  lod_loq = lod_result
)
tidy(report)
}
