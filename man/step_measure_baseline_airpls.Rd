% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/baseline-extended.R
\name{step_measure_baseline_airpls}
\alias{step_measure_baseline_airpls}
\title{Adaptive Iteratively Reweighted Penalized Least Squares Baseline}
\usage{
step_measure_baseline_airpls(
  recipe,
  measures = NULL,
  lambda = 1e+05,
  max_iter = 50L,
  tol = 0.001,
  role = NA,
  trained = FALSE,
  skip = FALSE,
  id = recipes::rand_id("measure_baseline_airpls")
)
}
\arguments{
\item{recipe}{A recipe object.}

\item{measures}{An optional character vector of measure column names.}

\item{lambda}{Smoothness parameter. Higher values produce smoother baselines.
Default is 1e5. Tunable via \code{\link[=baseline_lambda]{baseline_lambda()}}.}

\item{max_iter}{Maximum number of iterations. Default is 50.}

\item{tol}{Convergence tolerance for weight changes. Default is 1e-3.}

\item{role}{Not used.}

\item{trained}{Logical indicating if the step has been trained.}

\item{skip}{Logical. Should the step be skipped when baking?}

\item{id}{Unique step identifier.}
}
\value{
An updated recipe with the new step added.
}
\description{
\code{step_measure_baseline_airpls()} creates a \emph{specification} of a recipe step
that applies airPLS baseline correction. This method automatically adjusts
weights based on the difference between the signal and fitted baseline.
}
\details{
airPLS (Adaptive Iteratively Reweighted Penalized Least Squares) is an
improvement over standard ALS that automatically adapts the asymmetry
parameter based on the residuals. Key features:
\itemize{
\item No need to manually set asymmetry parameter
\item Good for signals with varying baseline curvature
\item Robust to different peak heights
}
}
\examples{
library(recipes)

rec <- recipe(water + fat + protein ~ ., data = meats_long) |>
  update_role(id, new_role = "id") |>
  step_measure_input_long(transmittance, location = vars(channel)) |>
  step_measure_baseline_airpls(lambda = 1e5) |>
  prep()

bake(rec, new_data = NULL)
}
\references{
Zhang, Z.M., Chen, S., & Liang, Y.Z. (2010). Baseline correction using
adaptive iteratively reweighted penalized least squares. Analyst, 135,
1138-1146.
}
\seealso{
Other measure-baseline: 
\code{\link{step_measure_baseline_als}()},
\code{\link{step_measure_baseline_custom}()},
\code{\link{step_measure_baseline_gpc}()},
\code{\link{step_measure_baseline_poly}()},
\code{\link{step_measure_baseline_py}()},
\code{\link{step_measure_baseline_rf}()},
\code{\link{step_measure_baseline_rolling}()},
\code{\link{step_measure_baseline_snip}()},
\code{\link{step_measure_detrend}()}
}
\concept{measure-baseline}
