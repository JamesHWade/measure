% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/criteria.R
\name{measure_criteria}
\alias{measure_criteria}
\title{Create a Set of Acceptance Criteria}
\usage{
measure_criteria(..., .list = NULL)
}
\arguments{
\item{...}{\code{\link[=criterion]{criterion()}} objects or named arguments that will be
converted to criteria. Named arguments use the format
\code{name = list(operator, threshold)} or \code{name = threshold} (assumes \code{"<="}).}

\item{.list}{Optional list of criterion objects.}
}
\value{
A \code{measure_criteria} object (list of \code{measure_criterion} objects).
}
\description{
Combines multiple \code{\link[=criterion]{criterion()}} objects into a criteria set for use
with \code{\link[=measure_assess]{measure_assess()}}.
}
\examples{
# Using criterion() objects
measure_criteria(
  criterion("cv_qc", "<", 15),
  criterion("r_squared", ">=", 0.99),
  criterion("recovery", "between", c(80, 120))
)

# Using shorthand notation
measure_criteria(
  cv_qc = list("<", 15),
  r_squared = list(">=", 0.99),
  bias = list("between", c(-10, 10))
)

# Simple threshold (assumes "<=")
measure_criteria(
  cv = 15,       # cv <= 15
  rsd = 20       # rsd <= 20
)

}
\seealso{
\code{\link[=criterion]{criterion()}} for creating individual criteria,
\code{\link[=measure_assess]{measure_assess()}} for evaluating criteria.
}
