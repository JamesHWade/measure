% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/method-comparison.R
\name{measure_proficiency_score}
\alias{measure_proficiency_score}
\title{Proficiency Testing Scores}
\usage{
measure_proficiency_score(
  data,
  measured_col,
  reference_col,
  uncertainty_col = NULL,
  reference_uncertainty_col = NULL,
  score_type = c("z_score", "en_score", "zeta_score"),
  sigma = NULL,
  group_col = NULL
)
}
\arguments{
\item{data}{A data frame containing measurement data.}

\item{measured_col}{Name of column with measured/reported values.}

\item{reference_col}{Name of column with reference/assigned values.}

\item{uncertainty_col}{Name of column with measurement uncertainties.
Required for En and zeta scores.}

\item{reference_uncertainty_col}{Name of column with reference value
uncertainties. Optional for En/zeta scores.}

\item{score_type}{Type of score to calculate:
\itemize{
\item \code{"z_score"} (default): (measured - reference) / sigma
\item \code{"en_score"}: (measured - reference) / sqrt(U_meas^2 + U_ref^2)
\item \code{"zeta_score"}: Similar to En, for correlated uncertainties
}}

\item{sigma}{Standard deviation for z-score calculation. If NULL,
estimated from the data.}

\item{group_col}{Optional grouping column for separate assessments.}
}
\value{
A \code{measure_proficiency_score} object containing:
\itemize{
\item \code{scores}: Tibble with individual scores and flags
\item \code{statistics}: Summary statistics and counts
}
}
\description{
Calculates proficiency testing scores (z-scores, En scores, or zeta scores)
for evaluating laboratory performance in interlaboratory comparisons.
}
\details{
\subsection{Score Interpretation}{

| |Score| | Status | Action |
|---------|---------------|--------|
| <= 2    | Satisfactory  | None   |
| 2-3     | Questionable  | Review |
| > 3     | Unsatisfactory| Investigate |
}

\subsection{Score Types}{

\strong{z-score}: Uses a fixed standard deviation (sigma), typically derived
from historical data or consensus of participants.

\strong{En score}: Uses expanded uncertainties of both the lab result and
reference value. Appropriate when uncertainties are well-characterized.

\strong{zeta score}: Similar to En, but accounts for potential correlation
between lab and reference uncertainties.
}
}
\examples{
# Proficiency testing results from multiple labs
pt_data <- data.frame(
  lab_id = paste0("Lab_", 1:10),
  measured = c(99.2, 100.5, 98.8, 101.2, 97.5, 100.1, 99.8, 102.3, 100.6, 94.0),
  assigned = rep(100, 10),
  uncertainty = c(1.5, 2.0, 1.8, 1.6, 2.2, 1.9, 1.7, 2.1, 1.5, 2.0)
)

# z-scores with known sigma
z_result <- measure_proficiency_score(
  pt_data,
  measured_col = "measured",
  reference_col = "assigned",
  score_type = "z_score",
  sigma = 2.5
)

print(z_result)

# En scores using uncertainties
en_result <- measure_proficiency_score(
  pt_data,
  measured_col = "measured",
  reference_col = "assigned",
  uncertainty_col = "uncertainty",
  score_type = "en_score"
)

print(en_result)
}
\seealso{
\code{\link[=measure_accuracy]{measure_accuracy()}}, \code{\link[=criteria_proficiency_testing]{criteria_proficiency_testing()}}

Other method-comparison: 
\code{\link{measure_bland_altman}()},
\code{\link{measure_deming_regression}()},
\code{\link{measure_passing_bablok}()}
}
\concept{method-comparison}
