% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/baseline-pls-advanced.R
\name{step_measure_baseline_iarpls}
\alias{step_measure_baseline_iarpls}
\title{Improved arPLS Baseline Correction (Two-Stage)}
\usage{
step_measure_baseline_iarpls(
  recipe,
  measures = NULL,
  lambda = 1e+06,
  lambda_1 = 10000,
  max_iter = 10L,
  tol = 1e-05,
  role = NA,
  trained = FALSE,
  skip = FALSE,
  id = recipes::rand_id("measure_baseline_iarpls")
)
}
\arguments{
\item{recipe}{A recipe object.}

\item{measures}{An optional character vector of measure column names.}

\item{lambda}{Final smoothness parameter. Default is 1e6.}

\item{lambda_1}{First stage (coarse) smoothness parameter. Default is 1e4.}

\item{max_iter}{Maximum number of iterations. Default is 10.}

\item{tol}{Convergence tolerance. Default is 1e-5.}

\item{role}{Not used.}

\item{trained}{Logical indicating if the step has been trained.}

\item{skip}{Logical. Should the step be skipped when baking?}

\item{id}{Unique step identifier.}
}
\value{
An updated recipe with the new step added.
}
\description{
\code{step_measure_baseline_iarpls()} creates a \emph{specification} of a recipe step
that applies Improved arPLS baseline correction using a two-stage approach.
}
\details{
iarpls uses a two-stage approach:
\enumerate{
\item First stage with smaller lambda for coarse baseline estimation
\item Second stage with larger lambda for refined baseline using weights
derived from the first stage
}

This approach often provides better results than single-stage arPLS for
signals with complex baseline patterns.
}
\examples{
library(recipes)

\donttest{
rec <- recipe(water + fat + protein ~ ., data = meats_long) |>
  update_role(id, new_role = "id") |>
  step_measure_input_long(transmittance, location = vars(channel)) |>
  step_measure_baseline_iarpls(lambda = 1e6, lambda_1 = 1e4) |>
  prep()

bake(rec, new_data = NULL)
}
}
\seealso{
Other measure-baseline: 
\code{\link{step_measure_baseline_airpls}()},
\code{\link{step_measure_baseline_als}()},
\code{\link{step_measure_baseline_arpls}()},
\code{\link{step_measure_baseline_aspls}()},
\code{\link{step_measure_baseline_auto}()},
\code{\link{step_measure_baseline_custom}()},
\code{\link{step_measure_baseline_fastchrom}()},
\code{\link{step_measure_baseline_gpc}()},
\code{\link{step_measure_baseline_minima}()},
\code{\link{step_measure_baseline_morph}()},
\code{\link{step_measure_baseline_morphological}()},
\code{\link{step_measure_baseline_poly}()},
\code{\link{step_measure_baseline_py}()},
\code{\link{step_measure_baseline_rf}()},
\code{\link{step_measure_baseline_rolling}()},
\code{\link{step_measure_baseline_snip}()},
\code{\link{step_measure_baseline_tophat}()},
\code{\link{step_measure_detrend}()}
}
\concept{measure-baseline}
