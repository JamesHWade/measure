% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/control-limits.R
\name{measure_control_limits}
\alias{measure_control_limits}
\title{Calculate Control Limits}
\usage{
measure_control_limits(
  data,
  value_col,
  group_col = NULL,
  type = c("shewhart", "ewma", "cusum"),
  n_sigma = 3,
  target = NULL,
  lambda = 0.2,
  k = 0.5,
  h = 5
)
}
\arguments{
\item{data}{A data frame containing QC measurements.}

\item{value_col}{Name of the column containing QC values.}

\item{group_col}{Optional grouping column (e.g., for different QC levels).}

\item{type}{Type of control chart: \code{"shewhart"} (default), \code{"ewma"}, or \code{"cusum"}.}

\item{n_sigma}{Number of standard deviations for control limits. Default is 3.}

\item{target}{Optional target value. If NULL, calculated from data mean.}

\item{lambda}{EWMA smoothing parameter (0 < lambda <= 1). Default is 0.2.}

\item{k}{CUSUM slack parameter. Default is 0.5 (in sigma units).}

\item{h}{CUSUM decision interval. Default is 5 (in sigma units).}
}
\value{
A \code{measure_control_limits} object containing:
\itemize{
\item \code{center}: Center line (target or mean)
\item \code{lcl}: Lower control limit
\item \code{ucl}: Upper control limit
\item \code{lwl}: Lower warning limit (2 sigma)
\item \code{uwl}: Upper warning limit (2 sigma)
\item \code{sigma}: Estimated standard deviation
\item Additional statistics depending on chart type
}
}
\description{
Calculates control limits for quality control monitoring using Shewhart
rules and optionally EWMA or CUSUM statistics.
}
\details{
\subsection{Shewhart Charts}{

Classic control charts with limits at mean +/- n*sigma:
\itemize{
\item UCL/LCL: Action limits (typically 3 sigma)
\item UWL/LWL: Warning limits (typically 2 sigma)
}
}

\subsection{EWMA Charts}{

Exponentially weighted moving average, more sensitive to small shifts:
\itemize{
\item Control limits narrow as more data is collected
\item Lambda parameter controls weight of recent observations
}
}

\subsection{CUSUM Charts}{

Cumulative sum chart for detecting persistent shifts:
\itemize{
\item Upper and lower CUSUM statistics track cumulative deviations
\item Decision interval h determines sensitivity
}
}
}
\examples{
# Calculate Shewhart control limits
set.seed(123)
qc_data <- data.frame(
  run_order = 1:30,
  qc_value = rnorm(30, mean = 100, sd = 2)
)
limits <- measure_control_limits(qc_data, "qc_value")
print(limits)

# EWMA control limits
limits_ewma <- measure_control_limits(qc_data, "qc_value", type = "ewma")
}
\seealso{
\code{\link[=measure_control_chart]{measure_control_chart()}}, \code{\link[=measure_system_suitability]{measure_system_suitability()}}

Other control-charts: 
\code{\link{measure_control_chart}()},
\code{\link{measure_system_suitability}()}
}
\concept{control-charts}
