% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nd_reduce.R
\name{measure_slice}
\alias{measure_slice}
\title{Extract slices from n-dimensional measurement}
\usage{
measure_slice(x, ..., drop = TRUE)
}
\arguments{
\item{x}{A \code{measure_nd_tbl} or \code{measure_nd_list} object.}

\item{...}{Named arguments specifying slice conditions. Names should be
dimension numbers (e.g., \code{dim_1 = 5}) or dimension names if set
(e.g., \code{time = 5}). Values can be:
\itemize{
\item A single value: exact match
\item A numeric vector: match any of these values
\item A function: applied to coordinates, should return logical
}}

\item{drop}{Logical. If \code{TRUE} (default), dimensions with a single
value are dropped from the result. If \code{FALSE}, they are retained.}
}
\value{
A \code{measure_tbl}, \code{measure_nd_tbl}, \code{measure_list}, or
\code{measure_nd_list} depending on the number of remaining dimensions.
}
\description{
Fixes one or more dimensions at specific coordinate values or ranges,
returning a lower-dimensional result.
}
\examples{
# Create a 3D measurement (2 x 3 x 4)
m3d <- new_measure_nd_tbl(
  location_1 = rep(1:2, each = 12),
  location_2 = rep(rep(1:3, each = 4), 2),
  location_3 = rep(1:4, 6),
  value = 1:24,
  dim_names = c("sample", "time", "wavelength")
)

# Extract slice at sample = 1
slice_2d <- measure_slice(m3d, dim_1 = 1)
measure_ndim(slice_2d)  # 2D

# Extract at specific time points
slice_subset <- measure_slice(m3d, dim_2 = c(1, 3))

# Use dimension names
slice_wl <- measure_slice(m3d, wavelength = 2)

}
