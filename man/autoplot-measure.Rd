% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{autoplot-measure}
\alias{autoplot-measure}
\alias{autoplot.measure_tbl}
\alias{autoplot.measure_list}
\alias{autoplot.recipe}
\title{Autoplot Methods for Measure Objects}
\usage{
\method{autoplot}{measure_tbl}(object, ...)

\method{autoplot}{measure_list}(
  object,
  summary = FALSE,
  max_spectra = 50,
  alpha = 0.3,
  color_by = NULL,
  ...
)

\method{autoplot}{recipe}(object, n_samples = 10, which = c("before_after", "summary"), ...)
}
\arguments{
\item{object}{A \code{measure_tbl}, \code{measure_list}, or \code{recipe} object.}

\item{...}{Additional arguments passed to specific plot types.}

\item{summary}{Logical. If TRUE, add mean +/- SD ribbon. Default FALSE.}

\item{max_spectra}{Maximum number of individual spectra to plot.
Default 50. Set to NULL for no limit.}

\item{alpha}{Transparency for individual spectrum lines. Default 0.3.}

\item{color_by}{Optional column name for coloring spectra by group.}

\item{n_samples}{Number of samples to show in before/after comparison.
Default 10.}

\item{which}{Which comparison to show: "before_after" (default),
"steps" (show each step), or "summary" (summary statistics).}
}
\value{
A ggplot2 object.
}
\description{
Create ggplot2 visualizations of spectral/chromatographic data stored
in measure objects.
}
\details{
For \code{measure_tbl} (single spectrum):
\itemize{
\item Plots location vs value as a line
}

For \code{measure_list} (multiple spectra):
\itemize{
\item Plots all spectra with optional summary ribbon
\item Use \code{summary = TRUE} for mean +/- SD ribbon
\item Use \code{max_spectra} to limit number of individual lines
}

For \code{recipe}:
\itemize{
\item Shows before/after comparison of preprocessing
\item Requires a prepped recipe
\item Use \code{n_samples} to control number of samples shown
}
}
\examples{
\dontrun{
library(ggplot2)

# Single spectrum
spec <- new_measure_tbl(location = 1:100, value = sin(1:100 / 10) + rnorm(100, sd = 0.1))
autoplot(spec)

# Multiple spectra with summary
rec <- recipe(water ~ ., data = meats_long) |>
  step_measure_input_long(transmittance, location = vars(channel)) |>
  prep()
baked <- bake(rec, new_data = NULL)
autoplot(baked$.measures, summary = TRUE)

# Recipe before/after comparison
rec <- recipe(water ~ ., data = meats_long) |>
  update_role(id, new_role = "id") |>
  step_measure_input_long(transmittance, location = vars(channel)) |>
  step_measure_snv() |>
  prep()
autoplot(rec, n_samples = 10)
}

}
