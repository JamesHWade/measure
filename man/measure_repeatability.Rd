% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/precision.R
\name{measure_repeatability}
\alias{measure_repeatability}
\title{Repeatability (Within-Run Precision)}
\usage{
measure_repeatability(data, response_col, group_col = NULL, conf_level = 0.95)
}
\arguments{
\item{data}{A data frame containing replicate measurements.}

\item{response_col}{Name of the column containing the response values.}

\item{group_col}{Optional name of a grouping column (e.g., concentration level).
If provided, repeatability is calculated within each group.}

\item{conf_level}{Confidence level for intervals. Default is 0.95.}
}
\value{
A \code{measure_precision} object containing:
\itemize{
\item \code{mean}: Mean of the replicates
\item \code{sd}: Standard deviation
\item \code{cv}: Coefficient of variation (\%)
\item \code{n}: Number of replicates
\item \code{se}: Standard error
\item \code{ci_lower}, \code{ci_upper}: Confidence interval for the mean
}
}
\description{
Calculates repeatability statistics for replicate measurements performed
under identical conditions (same operator, instrument, short time interval).
}
\details{
Repeatability represents the precision of a method under constant conditions
over a short time interval. It is typically assessed using at least 6
replicates of a sample at each concentration level of interest.

The coefficient of variation (CV) is reported as a percentage:
\code{CV = 100 * SD / mean}
}
\examples{
# Simple repeatability from replicate measurements
data <- data.frame(
  sample_id = rep("QC1", 10),
  concentration = rnorm(10, mean = 100, sd = 2)
)
measure_repeatability(data, "concentration")

# Repeatability at multiple concentration levels
data <- data.frame(
  level = rep(c("low", "mid", "high"), each = 6),
  concentration = c(
    rnorm(6, 10, 0.5),
    rnorm(6, 50, 2),
    rnorm(6, 100, 4)
  )
)
measure_repeatability(data, "concentration", group_col = "level")
}
\seealso{
\code{\link[=measure_intermediate_precision]{measure_intermediate_precision()}}, \code{\link[=measure_reproducibility]{measure_reproducibility()}}

Other precision: 
\code{\link{measure_gage_rr}()},
\code{\link{measure_intermediate_precision}()},
\code{\link{measure_reproducibility}()}
}
\concept{precision}
