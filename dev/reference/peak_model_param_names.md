# Get Parameter Names from Peak Model

Get Parameter Names from Peak Model

## Usage

``` r
peak_model_param_names(model)
```

## Arguments

- model:

  A `peak_model` object.

## Value

Character vector of parameter names.
